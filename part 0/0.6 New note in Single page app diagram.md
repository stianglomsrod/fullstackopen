sequenceDiagram
    participant browser
    participant server

    Note right of browser: User submits a new note
    Note right of browser: JavaScript prevents default (submission)
    Note right of browser: JavaScript creates the note object and redraws the notes list

    browser->>server: POST /exampleapp/new_note_spa
    activate server
    Note left of server: Request body contains the note as JSON
    server-->>browser: 201 Created
    deactivate server

    Note right of browser: No page reload, no redirect, no additional requests